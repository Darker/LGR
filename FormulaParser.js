/* implicit formula parser. Expects:
  negation
  bracket (beginning of complex formula)
  letter (logic variable)
*/
class FormulaParser {
    constructor() {
        this.currentSymbol = null;
    }
    parse(str) {
        var currentSymbol;
        for(var i=0, l=str.length; i<l; ++i) {
            
        }
    }
}